sudo service apache2 status -> for getting the apache server status
                               if it is running the close it first
sudo service apache2 stop -> stops apache server

then start lampp server

sudo /opt/lampp/lampp start
